[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "s1-chunked-prefill"
version = "0.1.0"
description = "Client and analysis workspace for fixed chunked-prefill experiments on Orin"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
